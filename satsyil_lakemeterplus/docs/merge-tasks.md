# Merge Plan — Combining `lakemeter-oss` and `databricks-cost-observability` into `satsyil_lakemeterplus`

## Strategy

Use **`lakemeter-oss`'s backend/frontend as the host application** (more modern stack: React/TS SPA, SQLAlchemy ORM, structured routers/services/models, real test suite, active Databricks Labs project) and **graft `databricks-cost-observability`'s domain logic in as a new "Observability" module** — new routers under `/api/v1/observability/*`, a ported `core/sql_executor.py` (Statement Execution API client) and `services/*` files kept largely intact, and new frontend pages/nav entries. This avoids a risky rewrite of either app's data-access layer (Postgres/SQLAlchemy vs. UC Statement-Execution-API are not worth unifying) while giving users one deployed app with both estimation and observability.

Legend: **P0** = required before anything else works, **P1** = required for a usable merged v1, **P2** = polish/parity, **P3** = nice-to-have/future. **Status** reflects what's actually landed in `satsyil_lakemeterplus/src` — see `TODO.md` for the fuller narrative and verification notes.

## Task table

| # | Priority | Status | Task | Depends on | Notes |
|---|---|---|---|---|---|
| 1 | P0 | **Done** | Resolve licensing conflict (Elastic License 2.0 vs. Databricks License) before combining source | — | Resolved by explicit user decision: adopt the more permissive of the two (Elastic License 2.0 — it doesn't tie use to being a customer of a specific vendor's paid service, unlike the Databricks License's "in connection with your use of the Databricks Services" restriction). Implemented as `../LICENSE` + `../NOTICE.md` (the latter preserves upstream attribution/notice-preservation obligations from both original licenses). This is a good-faith technical reading, not legal advice — see `TODO.md`. |
| 2 | P0 | **Done** | Scaffold unified `src/` project: copy `lakemeter-oss/backend` as `src/backend`, copy `lakemeter-oss/frontend` as `src/frontend` | 1 (informational only) | Host app baseline. Build artifacts (`dist/`, `node_modules/`, compiled `static/assets`) intentionally excluded. |
| 3 | P0 | **Done** | Namespace-port `databricks-cost-observability`'s `core/sql_executor.py`, `core/validators.py`, `core/subscriptions.py` into `src/backend/app/observability/core/` | 2 | Kept separate from Lakemeter's own `validators.py` to avoid symbol collisions (`config.py` was later unified — see #9). |
| 4 | P0 | **Done** | Port `services/*.py` (17 files) into `src/backend/app/observability/services/`, update imports to the new `core.observability.*` paths | 3 | Business logic is self-contained per file; mechanical import rewrite. |
| 5 | P0 | **Done** | Port `api/v1/*.py` (20 files) into `src/backend/app/observability/routes/`, mount under `/api/v1/observability` in `src/backend/app/main.py` | 4 | Preserved `_svc()`/`_handle_async()` DI pattern; observability sub-routers keep their own prefixes (`/cost`, `/admin`, ...) under the shared `/observability` prefix, e.g. `/api/v1/observability/cost/summary`. Verified by actually importing the merged app: 136 total routes, 48 under `/api/v1/observability/*`, zero collisions with Lakemeter's own routes. |
| 6 | P0 | **Done** | Add `WorkspaceClient`/`AccountClient` FastAPI dependencies (`get_workspace_client`, `get_account_client`) alongside Lakemeter's existing `token_manager`/Lakebase dependencies | 2 | Came from the ported `observability/core/dependencies.py` as-is. |
| 7 | P1 | **Partial** | Reconcile the two auth/identity systems into one `auth/` module | 2 | Done: the two modules' header lists (`app.auth.databricks_auth.EMAIL_HEADERS`/`USER_HEADERS` and `app.observability.core.dependencies._USER_HEADERS`) were extended to the **union** of both, so either module now recognizes headers the other expects — closes a real bug where a real user's identity could silently resolve to empty in one module depending on which header Databricks Apps sent. **Not done:** the two modules still each run their own resolver function and identity-of-record concept (Lakemeter's DB-backed `User` row vs. observability's stateless per-request resolution) — a single shared `resolve_user_identity()` is still future work. |
| 8 | P1 | **Partial** | Merge admin/allowlist model with Lakemeter's user model | 7 | Resolved the mechanical part without making the app-wide product decision: `app/observability/core/allowlist.py` (`ObservabilityAllowlistMiddleware`, ported from cost-observability's `app.py`) enforces `ALLOWED_USERS` on `/api/v1/observability/*` only, wired into `main.py`. Lakemeter's own routes are provably unaffected (see `../src/tests/observability/test_allowlist.py`). **Still open:** whether `ALLOWED_USERS`/`ADMIN_USERS` should also gate Lakemeter's estimation routes is a product decision, not resolved here. |
| 9 | P1 | **Done** | Merge `config.py`: extend Lakemeter's Pydantic `Settings` with cost-observability's fields (`MOCK_MODE`, `UC_CATALOG_NAME`, `DATABRICKS_WAREHOUSE_ID`, `ALLOWED_USERS`, `ADMIN_USERS`, `ALLOWED_WORKSPACE_IDS`, SMTP/alert vars) | 2 | All of the observability module's settings fields and property helpers (`admin_users_set`, `user_configs_dict`, etc.) were folded into `app/config.py`'s `Settings`. `app/observability/core/config.py` is now a thin shim (`get_settings()` returns the same `app.config.settings` instance) so the ~14 call sites across observability's routes/services didn't need individual rewrites. Verified: `app.observability.core.config.get_settings() is app.config.settings` returns `True` at runtime. |
| 10 | P1 | **Done** (as-is) | Add `MOCK_MODE` support to the merged `sql_executor` so the observability module still runs demo-able on any workspace | 3 | `MOCK_MODE` was already read directly from the process environment at import time in `sql_executor.py` (not through the `Settings` class), so it needed no change to keep working — it's wired into the merged `app.yaml`. Not yet exercised end-to-end (needs a real/mock warehouse). |
| 11 | P1 | **Done** | Port mock/demo data scripts (`setup_mock_tables.py`, `setup_enterprise_mock_data.py`) into `src/backend/app/observability/scripts/`, parameterize the hardcoded warehouse ID (`21f5bd20b7f44a51`) via env/config | 10 | The `os.environ.get("DATABRICKS_WAREHOUSE_ID", "21f5bd20b7f44a51")` fallback in all three scripts (`send_cost_alerts.py`, `setup_mock_tables.py`, `setup_enterprise_mock_data.py`) was changed to a required `os.environ["DATABRICKS_WAREHOUSE_ID"]`, consistent with how `DATABRICKS_HOST`/`DATABRICKS_TOKEN` already fail loudly. The *mock-data-literal* uses of that same ID string inside `setup_mock_tables.py` (seeding a fake warehouse row and its self-referencing query-history rows) were left as-is — those are internally-consistent demo data, not real configuration. |
| 12 | P1 | **Partial** | Frontend: add an "Observability" section to the React app — new pages under `src/frontend/src/pages/` + nav entries in `Layout.tsx`, calling the new `/api/v1/observability/*` endpoints | 5 | One page shipped as a proof of concept: `src/frontend/src/pages/Observability.tsx` (KPI cards for `/observability/cost/summary`, raw-JSON preview of `/observability/executive/summary`), `src/frontend/src/api/observability.ts` (typed client), a `/observability` route in `App.tsx`, and a nav entry in `Layout.tsx`. **Not done:** the other ~16 observability domains (Compute, Query Attribution, Access Graph, Storage, Lineage, Governance, Cluster Health, Marketplace, Platform Ops, Cloud Cost, Alerts, Admin, ML Anomalies) have no frontend yet — build incrementally, reusing Chart.js/D3/3d-force-graph as embedded components. Could not `npm run build`/type-check in this session (no Node.js in the environment) — reviewed by hand instead; see `TODO.md`. |
| 13 | P1 | **Partial** | Merge deployment config: single `databricks.yml` DAB covering Lakebase provisioning (Lakemeter) **and** the app's UC/SQL-Warehouse permissions (observability) in one bundle | 2, 9 | Added a `variables:` block (`warehouse_id`, `catalog_name`, `mock_mode`, `admin_users`) and `dev`/`prod` targets to `src/scripts/databricks.yml`, threaded through as new job parameters into the `create_app` task, which is consumed by `scripts/notebooks/05a_create_app.py` — that notebook (not the static `backend/app.yaml`) is what actually generates the deployed app's `app.yaml` when installed via `scripts/install.sh` / the DAB job, and it now writes `MOCK_MODE`, `DATABRICKS_WAREHOUSE_ID`, `UC_CATALOG_NAME`, `ADMIN_USERS`, `ALLOWED_USERS`, `ALLOWED_WORKSPACE_IDS` into it. **Deliberately not ported**: cost-observability's `resources.apps:` bundle resource + its `permissions:` block (`CAN_USE`/`CAN_MANAGE`) — Lakemeter's app is created *imperatively* via SDK calls inside a notebook, not declared as a DAB resource, and declaring both would fight over ownership of the same app. App-level workspace ACLs aren't set up by either installer path today. **Also not ported**: the actual `install.sh` / `scripts/install.sh` / `scripts/install_lakemeter.py` shell/Python installer entrypoints were never copied into `src/` at all (only `databricks.yml` + `notebooks/` + `functions/` were) — see `TODO.md`. Verified: extracted the notebook's `app_yaml_content` f-string template, rendered it with realistic sample values in a throwaway script, and confirmed the output is valid YAML containing exactly the expected observability env vars. `databricks.yml` itself YAML-validated and its parameter/target wiring inspected structurally with `pyyaml`. |
| 14 | P1 | **Done** | Merge `app.yaml`: union of both apps' env vars (Lakebase creds + Claude endpoint + warehouse/catalog/mock-mode/allowlist/SMTP vars) | 9, 13 | `src/backend/app.yaml` now unions both. The two hardcoded environment-specific values found in the source repos (a workspace URL in Lakemeter's, the demo warehouse ID in cost-observability's) were replaced with empty values / `valueFrom` secret placeholders rather than carried forward. |
| 15 | P2 | **Done** | De-duplicate alert-sending logic (`services/alert_service.py` vs. standalone `scripts/send_cost_alerts.py`) into one shared module callable both from the API and as a scheduled-job entrypoint | 4 | Extracted `app/observability/services/alert_email.py` (SMTP config validation, email sending, HTML template) from what used to be duplicated inline in `routes/alerts.py` and reimplemented independently in `scripts/send_cost_alerts.py`. The script now imports `AlertService` + `alert_email` directly instead of maintaining its own SQL rewriter, `run_sql`, `detect_alerts`, and `build_email` — as a side effect it also gained `MOCK_MODE` support for free (previously it had its own separate reimplementation of the mock-table rewrite). Verified: script imports cleanly with no Lakebase/SQLAlchemy side effects, `/alerts/*` routes still respond correctly through the refactored route file. While doing this, found and fixed a real bug (see `TODO.md`): 12 lazy/function-local imports across the observability module were never rewritten by the original mechanical port and still pointed at the pre-merge `core.`/`services.` module paths — they would have raised `ModuleNotFoundError` the first time those specific code paths executed. All fixed and covered by tests. |
| 16 | P2 | Not started | Migrate `UserPermissionsService` from Delta-table storage to a Lakebase table (new SQLAlchemy model) for consistency with the rest of the merged app's data layer | 8 | Optional consolidation; Delta-table approach still works standalone if deprioritized. |
| 17 | P2 | Not started | Reconcile SKU/product-type mapping duplication: Lakemeter's `get_sku_type()` (Python) vs. its own `get_product_type_for_pricing()` (PL/pgSQL) vs. cost-observability's SKU handling in `services/cost_service.py`/`ai_service.py` (queries live `system.billing.list_prices`) | 4 | Two different pricing *sources* (Lakemeter's synced `sync_pricing_*` tables vs. observability's live `system.billing.list_prices` queries) will legitimately continue to coexist — estimation wants stable/cacheable prices, observability wants live actuals — but the SKU **naming/mapping** logic should be a single shared utility to avoid drift. |
| 18 | P2 | **Partial** | Unify testing strategy: bring cost-observability's black-box `smoke_test.py` pattern in as an `e2e`/`harness`-style suite consistent with Lakemeter's existing `tests/harness/` numbering, and add unit tests for the newly-ported observability services (currently zero unit-test coverage) | 4, 5 | `src/tests/` now has **77 tests, all passing** (`python -m pytest`, run for real, not just written): `test_merged_app_wiring.py` (route-collision, middleware-order, config-sharing), `observability/test_allowlist.py` (pins task #8's scoping), `observability/test_alert_service.py` (`AlertService.detect_spikes()`'s spike/baseline/new-SKU/growth math), `observability/test_alert_email.py` (SMTP config validation + HTML template rendering/escaping), `observability/test_validators.py` (the module's primary SQL-injection defense — identifier/date/email/threshold/tab-list/workspace-list validation, including explicit injection-payload rejection tests), `observability/test_sql_executor.py` (the `MOCK_MODE` table-rewrite regex logic that powers demo-mode). All of this had **zero** prior coverage in either source repo. **Not done:** neither source repo's own test suite was ported in; unit tests for the other ~13 observability services (compute, query, access, lakeflow, storage, lineage, governance, cluster_health, marketplace, platform_ops, ml, cost, executive, audit, cloud_cost) are still missing — `cost_service.py` and `executive_service.py` in particular are the two largest, most logic-heavy files in the module and would be the next-highest-value additions. |
| 19 | P2 | **Done** | Extend CI (`ci.yml`) to compile/test the new `observability` package and exercise the smoke-test-derived suite | 18 | `src/.github/workflows/ci.yml` added (adapted from Lakemeter's own `ci.yml`): a `python-checks` job (`compileall` + `pytest -v` over `src/tests/`) and a `frontend-build` job (`npm ci && npm run build`). YAML-validated with `pyyaml` in this session; not run through actual GitHub Actions. |
| 20 | P2 | Not started | Reconcile multi-cloud cost concepts: cost-observability's `cloud_cost.py` (actual multi-cloud *spend*) vs. Lakemeter's VM pricing tables (multi-cloud *pricing inputs* for estimation) — document as intentionally distinct, cross-link in UI (e.g., "compare your estimate to actual spend for this workspace") | 5, 12 | This is the one place a genuinely new cross-module feature (estimate-vs-actual comparison) becomes possible post-merge; worth a dedicated design doc once both modules are live. |
| 21 | P3 | Not started | Decide fate of cost-observability's standalone HTML smoke-test DOM-manifest checks (`TAB_ELEMENTS`, `JS_FUNCTIONS`) — not applicable once the UI is React-based; replace with component/E2E tests (e.g. Playwright) against the new observability pages | 12, 18 | |
| 22 | P3 | **Done** | Security-header/rate-limiting middleware: decide whether to adopt cost-observability's `AuditLogMiddleware` (tiered rate limiting, bot detection, progressive IP banning) app-wide, since Lakemeter currently has lighter security middleware | 2 | `HTTPSRedirectMiddleware`, `SecurityHeadersMiddleware`, and `AuditLogMiddleware` are wired into `src/backend/app/main.py`, app-wide, outermost-to-innermost ahead of CORS. Deliberately did **not** port `AllowlistMiddleware` (the actual authz gate) — that changes access-control semantics for Lakemeter's own routes too and is a product decision, not a mechanical port; see task #8 and `TODO.md`. Verified via the same import-and-inspect check used for task #5: `app.user_middleware` reports `['HTTPSRedirectMiddleware', 'SecurityHeadersMiddleware', 'AuditLogMiddleware', 'CORSMiddleware']` in that order. |
| 23 | P3 | Not started | Consolidate documentation sites: cost-observability's `docs/` (screenshots + marketplace listing) vs. Lakemeter's Docusaurus `docs-site/` — fold observability screenshots/marketplace copy into the Docusaurus site as a new section | 12 | |
| 24 | P3 | **Done** | Fix latent deploy bug carried in from cost-observability: `deploy.yml`'s prod path sourcing from the `dev` bundle's files path | 13 | `src/.github/workflows/deploy.yml` added — structurally avoids the bug rather than just not reproducing it: it never manually constructs a `.bundle/<app>/<target>/files`-style path at all (the source workflow's bug was hardcoding `/dev/` in exactly that kind of path regardless of the selected target). Instead it runs `databricks bundle deploy --target "$TARGET"` followed by `databricks bundle run lakemeter_installer --target "$TARGET" --params ...`, consistently passing `$TARGET` through and letting the CLI resolve paths internally — matching how the merged bundle actually deploys (a job that creates+deploys the app via SDK calls in a notebook, not a bare `databricks apps deploy --source-code-path` step). YAML- and shell-syntax-validated (`pyyaml` + `bash -n` on the extracted script bodies); not run through actual GitHub Actions. |

## Explicitly out of scope for this pass

- Rewriting either data-access layer (SQLAlchemy/Lakebase vs. Statement-Execution-API/Unity-Catalog) into a shared abstraction — architecturally sound to keep as two distinct data paths.
- A full 1:1 React port of the 8.2K-line `static/index.html` dashboard — see task #12, recommend incremental delivery.
- Resolving the Elastic License 2.0 vs. Databricks License conflict — legal/ownership decision, not an engineering one (task #1 flags it, does not resolve it).
