# Merge Plan — Combining `lakemeter-oss` and `databricks-cost-observability` into `satsyil_lakemeterplus`

## Strategy

Use **`lakemeter-oss`'s backend/frontend as the host application** (more modern stack: React/TS SPA, SQLAlchemy ORM, structured routers/services/models, real test suite, active Databricks Labs project) and **graft `databricks-cost-observability`'s domain logic in as a new "Observability" module** — new routers under `/api/v1/observability/*`, a ported `core/sql_executor.py` (Statement Execution API client) and `services/*` files kept largely intact, and new frontend pages/nav entries. This avoids a risky rewrite of either app's data-access layer (Postgres/SQLAlchemy vs. UC Statement-Execution-API are not worth unifying) while giving users one deployed app with both estimation and observability.

Legend: **P0** = required before anything else works, **P1** = required for a usable merged v1, **P2** = polish/parity, **P3** = nice-to-have/future.

## Task table

| # | Priority | Task | Depends on | Notes |
|---|---|---|---|---|
| 1 | P0 | Resolve licensing conflict (Elastic License 2.0 vs. Databricks License) before combining source | — | Not an engineering decision — needs explicit sign-off from whoever owns each repo; blocks distributing the merged codebase publicly. Flagged, not resolved, in this pass. |
| 2 | P0 | Scaffold unified `src/` project: copy `lakemeter-oss/backend` as `src/backend`, copy `lakemeter-oss/frontend` as `src/frontend` | 1 (informational only) | Host app baseline. |
| 3 | P0 | Namespace-port `databricks-cost-observability`'s `core/sql_executor.py`, `core/validators.py`, `core/subscriptions.py` into `src/backend/app/observability/core/` | 2 | Keep separate from Lakemeter's own `validators.py`/`config.py` to avoid symbol collisions; both modules named `validators.py` today. |
| 4 | P0 | Port `services/*.py` (17 files) into `src/backend/app/observability/services/`, update imports to the new `core.observability.*` paths | 3 | Business logic is self-contained per file; mechanical import rewrite. |
| 5 | P0 | Port `api/v1/*.py` (20 files) into `src/backend/app/observability/routes/`, mount under `/api/v1/observability` in `src/backend/app/main.py` | 4 | Preserve `_svc()`/`_handle_async()` DI pattern; rename router prefixes to avoid clashing with Lakemeter's `/cost`, `/admin` prefixes (e.g. observability's `/cost` → `/observability/cost`). |
| 6 | P0 | Add `WorkspaceClient`/`AccountClient` FastAPI dependencies (`get_workspace_client`, `get_account_client`) alongside Lakemeter's existing `token_manager`/Lakebase dependencies | 2 | Both apps already use the Databricks SDK; this just adds the client construction Lakemeter doesn't currently need. |
| 7 | P1 | Reconcile the two auth/identity systems into one `auth/` module | 2 | Lakemeter uses `X-Forwarded-Email`; cost-observability uses `X-Forwarded-User`/`X-Databricks-User-Email` + numeric-ID SCIM resolution. Merge into a single `resolve_user_identity()` that checks all known header variants, keep Lakemeter's `get_or_create_user()` DB upsert as the identity of record. |
| 8 | P1 | Merge admin/allowlist model with Lakemeter's user model | 7 | Add `ADMIN_USERS`/`ALLOWED_USERS` env-driven checks as an additional guard on observability routes; keep Lakemeter's per-user Postgres `users` table as-is for estimation routes. Decide whether the Delta-table-backed per-user tab/workspace restriction system (`UserPermissionsService`) is kept as-is (writes to a Delta table) or migrated to a Lakebase table for consistency — recommend migrating to Lakebase in a later pass (P2, see #16). |
| 9 | P1 | Merge `config.py`: extend Lakemeter's Pydantic `Settings` with cost-observability's fields (`MOCK_MODE`, `UC_CATALOG_NAME`, `DATABRICKS_WAREHOUSE_ID`, `ALLOWED_USERS`, `ADMIN_USERS`, `ALLOWED_WORKSPACE_IDS`, SMTP/alert vars) | 2 | Single source of truth for env config; avoid two competing `Settings` classes. |
| 10 | P1 | Add `MOCK_MODE` support to the merged `sql_executor` so the observability module still runs demo-able on any workspace | 3 | High-value differentiator from cost-observability worth preserving. |
| 11 | P1 | Port mock/demo data scripts (`setup_mock_tables.py`, `setup_enterprise_mock_data.py`) into `src/backend/scripts/observability/`, parameterize the hardcoded warehouse ID (`21f5bd20b7f44a51`) via env/config | 10 | Also fix while porting — don't carry the hardcoded ID forward. |
| 12 | P1 | Frontend: add an "Observability" section to the React app — new pages under `src/frontend/src/pages/observability/` + nav entries in `Layout.tsx`, calling the new `/api/v1/observability/*` endpoints | 5 | This is the largest single piece of net-new frontend work — replacing 8.2K lines of vanilla-JS/Chart.js/D3 tabs with React equivalents is not a 1:1 port; recommend building it incrementally, tab-by-tab, reusing Chart.js/D3/3d-force-graph as embedded components rather than rewriting the visualizations from scratch. |
| 13 | P1 | Merge deployment config: single `databricks.yml` DAB covering Lakebase provisioning (Lakemeter) **and** the app's UC/SQL-Warehouse permissions (observability) in one bundle | 2, 9 | Combine Lakemeter's 9-task job DAG with cost-observability's simpler `apps` resource + warehouse/catalog variables. |
| 14 | P1 | Merge `app.yaml`: union of both apps' env vars (Lakebase creds + Claude endpoint + warehouse/catalog/mock-mode/allowlist/SMTP vars) | 9, 13 | Watch for name collisions (e.g. both define `ENVIRONMENT`, `CORS_ORIGINS`). |
| 15 | P2 | De-duplicate alert-sending logic (`services/alert_service.py` vs. standalone `scripts/send_cost_alerts.py`) into one shared module callable both from the API and as a scheduled-job entrypoint | 4 | Pre-existing duplication inside cost-observability itself; fix during the port rather than carrying it forward. |
| 16 | P2 | Migrate `UserPermissionsService` from Delta-table storage to a Lakebase table (new SQLAlchemy model) for consistency with the rest of the merged app's data layer | 8 | Optional consolidation; Delta-table approach still works standalone if deprioritized. |
| 17 | P2 | Reconcile SKU/product-type mapping duplication: Lakemeter's `get_sku_type()` (Python) vs. its own `get_product_type_for_pricing()` (PL/pgSQL) vs. cost-observability's SKU handling in `services/cost_service.py`/`ai_service.py` (queries live `system.billing.list_prices`) | 4 | Two different pricing *sources* (Lakemeter's synced `sync_pricing_*` tables vs. observability's live `system.billing.list_prices` queries) will legitimately continue to coexist — estimation wants stable/cacheable prices, observability wants live actuals — but the SKU **naming/mapping** logic should be a single shared utility to avoid drift. |
| 18 | P2 | Unify testing strategy: bring cost-observability's black-box `smoke_test.py` pattern in as an `e2e`/`harness`-style suite consistent with Lakemeter's existing `tests/harness/` numbering, and add unit tests for the newly-ported observability services (currently zero unit-test coverage) | 4, 5 | Cost-observability contributes almost no unit tests; this is where most new test-writing effort goes. |
| 19 | P2 | Extend CI (`ci.yml`) to compile/test the new `observability` package and exercise the smoke-test-derived suite | 18 | |
| 20 | P2 | Reconcile multi-cloud cost concepts: cost-observability's `cloud_cost.py` (actual multi-cloud *spend*) vs. Lakemeter's VM pricing tables (multi-cloud *pricing inputs* for estimation) — document as intentionally distinct, cross-link in UI (e.g., "compare your estimate to actual spend for this workspace") | 5, 12 | This is the one place a genuinely new cross-module feature (estimate-vs-actual comparison) becomes possible post-merge; worth a dedicated design doc once both modules are live. |
| 21 | P3 | Decide fate of cost-observability's standalone HTML smoke-test DOM-manifest checks (`TAB_ELEMENTS`, `JS_FUNCTIONS`) — not applicable once the UI is React-based; replace with component/E2E tests (e.g. Playwright) against the new observability pages | 12, 18 | |
| 22 | P3 | Security-header/rate-limiting middleware: decide whether to adopt cost-observability's `AuditLogMiddleware` (tiered rate limiting, bot detection, progressive IP banning) app-wide, since Lakemeter currently has lighter security middleware | 2 | Net-positive hardening; low coupling, can be ported largely as-is. |
| 23 | P3 | Consolidate documentation sites: cost-observability's `docs/` (screenshots + marketplace listing) vs. Lakemeter's Docusaurus `docs-site/` — fold observability screenshots/marketplace copy into the Docusaurus site as a new section | 12 | |
| 24 | P3 | Fix latent deploy bug carried in from cost-observability: `deploy.yml`'s prod path sourcing from the `dev` bundle's files path | 13 | Small but real bug worth not reproducing in the merged pipeline. |

## Explicitly out of scope for this pass

- Rewriting either data-access layer (SQLAlchemy/Lakebase vs. Statement-Execution-API/Unity-Catalog) into a shared abstraction — architecturally sound to keep as two distinct data paths.
- A full 1:1 React port of the 8.2K-line `static/index.html` dashboard — see task #12, recommend incremental delivery.
- Resolving the Elastic License 2.0 vs. Databricks License conflict — legal/ownership decision, not an engineering one (task #1 flags it, does not resolve it).
